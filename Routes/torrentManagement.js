module.exports = {
    findBestTorrent: async (req, res) => {
        // SORT TORRENT BY BEST SEED
        // await movies.sort((a, b) => {
        //     return b.seeds - a.seeds;
        // });
        // QUALITY IN REGEX
        // movie.quality = found ? found[1] : undefined;

    }
};